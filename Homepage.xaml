<Page x:Class="SnakeSpiel.Homepage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="Homepage" Background="#1A1A2E">

    <Page.Resources>
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#303050"/>
            <Setter Property="BorderBrush" Value="#4A4A8A"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="10,15"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4A4A8A"/>
                    <Setter Property="BorderBrush" Value="#00FFCC"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid>
        <StackPanel Width="300" VerticalAlignment="Center" HorizontalAlignment="Center">

            <TextBlock Text="SNAKE" Foreground="White" FontSize="72" FontFamily="Consolas" 
                       FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,60">
                <TextBlock.Effect>
                    <DropShadowEffect Color="#4A4A8A" BlurRadius="15" ShadowDepth="0"/>
                </TextBlock.Effect>
            </TextBlock>

            <Button x:Name="Button_Start" Content="NEW GAME" Style="{StaticResource MenuButtonStyle}" Click="Button_Start_Click"/>
            <Button x:Name="Button_Continue" Content="CONTINUE" Style="{StaticResource MenuButtonStyle}" Click="Button_Continue_Click"/>
            <Button x:Name="Button_Load" Content="LOAD" Style="{StaticResource MenuButtonStyle}" Click="Button_Load_Click"/>
            <Button x:Name="Button_Scoreboard" Content="SCOREBOARD" Style="{StaticResource MenuButtonStyle}" Click="Button_Scoreboard_Click"/>
            <Button x:Name="Button_Settings" Content="SETTINGS" Style="{StaticResource MenuButtonStyle}" Click="Button_Settings_Click" Margin="0"/>

        </StackPanel>
    </Grid>
</Page>